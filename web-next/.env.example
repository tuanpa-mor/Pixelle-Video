# Local dev environment for the Next.js web client.
# Copy to .env and adjust per environment.
# This file is safe to commit (no secrets).

# Absolute URL the browser uses to reach the FastAPI backend.
# In local dev the API runs on port 8001 (Docker API service or uv run).
NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8001
